"use strict"

class Point {
    constructor(x, y) {
        this.x = parseInt(x, 10);
        this.y = parseInt(y, 10);
    }
}

class PointF {
    constructor(x, y) {
        this.x = x;
        this.y = y;
    }
}